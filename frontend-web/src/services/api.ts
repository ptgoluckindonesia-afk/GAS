import axios from 'axios';\n\nconst API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000/api';\n\nconst api = axios.create({\n  baseURL: API_URL,\n  timeout: 10000,\n});\n\n// Add token to request headers\napi.interceptors.request.use((config) => {\n  const token = localStorage.getItem('token');\n  if (token) {\n    config.headers.Authorization = `Bearer ${token}`;\n  }\n  return config;\n});\n\n// Auth API\nexport const authAPI = {\n  register: (email: string, name: string, password: string) =>\n    api.post('/auth/register', { email, name, password }),\n  login: (email: string, password: string) =>\n    api.post('/auth/login', { email, password }),\n  logout: () => api.post('/auth/logout'),\n  getCurrentUser: () => api.get('/auth/me'),\n};\n\n// User API\nexport const userAPI = {\n  getAllUsers: () => api.get('/users'),\n  getUserById: (id: string) => api.get(`/users/${id}`),\n  updateProfile: (name: string, avatar: string) =>\n    api.put('/users/profile', { name, avatar }),\n  searchUsers: (query: string) => api.get(`/users/search?q=${query}`),\n};\n\n// Chat API\nexport const chatAPI = {\n  getChats: () => api.get('/chats'),\n  createChat: (participantIds: string[], groupName?: string, isGroup?: boolean) =>\n    api.post('/chats', { participantIds, groupName, isGroup }),\n  getMessages: (chatId: string, limit = 50, offset = 0) =>\n    api.get(`/chats/${chatId}/messages?limit=${limit}&offset=${offset}`),\n  sendMessage: (chatId: string, content: string) =>\n    api.post('/chats/messages/send', { chatId, content }),\n  markAsRead: (messageId: string) =>\n    api.put('/chats/messages/read', { messageId }),\n};\n\nexport default api;\n"
